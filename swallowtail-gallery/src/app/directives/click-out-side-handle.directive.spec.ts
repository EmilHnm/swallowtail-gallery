import { ClickOutSideHandleDirective } from './click-out-side-handle.directive';

describe('ClickOutSideHandleDirective', () => {
  it('should create an instance', () => {
    const directive = new ClickOutSideHandleDirective();
    expect(directive).toBeTruthy();
  });
});
